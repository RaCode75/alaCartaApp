export interface Platoi {
    id?: number,
    title?: string,
    image?: string,
    readyInMinutes?: number,
    healthScore?: number,
    pricePerServing?: number,
    summary?: string,
    vegan?: boolean
}