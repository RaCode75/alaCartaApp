export interface Platoi {
    id?: number | undefined,
    title?: string,
    image?: string,
    readyInMinutes?: number,
    healthScore?: number,
    pricePerServing?: number,
    summary?: string,
    vegan?: boolean
}